name: ci
on:
  pull_request:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: deliberately failing assertion (verify-loop iteration E2E)
        run: |
          echo "this workflow always fails — testing verify-loop iteration cap"
          echo "expected greeter content (impossible-to-produce phrase): UNREACHABLE_BY_AI_zKvL9q3w"
          echo "actual content:"
          cat src/greeter.ts
          exit 1
